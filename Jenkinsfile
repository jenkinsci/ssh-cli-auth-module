// Builds a module using https://github.com/jenkins-infra/pipeline-library
buildPlugin()
// not using configurations: buildPlugin.recommendedConfigurations() since baseline is currently newer than last LTS
